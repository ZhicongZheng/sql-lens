# Parent Design

Two independent deliverables ordered for verification:

1. Identity write-back (protocol + lifecycle + app store).
2. Default static UI delivery (docs + config wiring verification + build path).

Parent owns cross-child acceptance: both land, workspace gate for touched areas, no plugin/TLS expansion.
