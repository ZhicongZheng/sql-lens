import { PageStub } from "@/components/layout/page-stub";

export function SqlEventsRoute() {
  return (
    <PageStub
      title="SQL"
      description="Live SQL event list with filters, pause, and slow/error highlighting."
    />
  );
}
