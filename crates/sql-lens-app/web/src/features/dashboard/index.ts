// Feature barrels are intentionally empty placeholders. UI.md reserves a
// feature folder per top-level view; concrete implementations land in
// follow-up issues. This file exists so the directory is tracked by git.
export {};
